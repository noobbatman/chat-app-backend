services:
  - type: web
    name: chat-app-backend
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: JAVA_OPTS
        value: "-Xms512m -Xmx1024m"
